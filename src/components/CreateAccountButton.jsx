import React from "react";

const CreateAccountButton = () => {
  return (
    <a
      href="#"
      className="bg-gradient-to-r from-orange-500 to-red-800 hover:from-orange-600 hover:to-red-900 hover:transition-colors duration-500 py-2 px-3 rounded-md"
    >
      Create an account
    </a>
  );
};

export default CreateAccountButton;
